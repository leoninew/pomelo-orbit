# A short-lived JWT for one non-admin Pomelo Orbit user with CI read access.
# This project deliberately does not accept a username/password or persist a token.
POMELO_PIPELINE_URL=http://127.0.0.1:8080
POMELO_PIPELINE_JWT=replace-with-a-short-lived-user-jwt
