# Generated by scripts/render_ragflow_deployment_contract.py from scripts/ragflow-integrated/deployment-contract.json.
# Do not edit this file directly.
# CPU reference for the default Orbit deployment mode: one RAGFlow Application with six Components.
# Do not use this file when the user explicitly requests split applications.
# Each bundled component owns a separate directory below ./data, matching the Orbit logical-mount layout.
# GPU overlay for the bundled RAGFlow + TEI Compose reference.
# Use only on a host with NVIDIA Driver and NVIDIA Container Toolkit.
# Orbit lifecycle operations remain MCP-only.
services:
  tei:
    image: "ghcr.io/huggingface/text-embeddings-inference:cuda-1.9.3@sha256:249a0bc87522bfe2f1012b4d194f0225878f47079115ada3aeb0b1ef257b402a"
    deploy:
      resources:
        reservations:
          devices:
            -
              driver: nvidia
              count: all
              capabilities:
                - gpu
