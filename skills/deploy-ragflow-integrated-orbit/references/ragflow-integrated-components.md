# RAGFlow Integrated Deployment Primitives
> Generated by scripts/render_ragflow_deployment_contract.py from scripts/ragflow-integrated/deployment-contract.json. Do not edit this file directly.

Use this reference before an integrated RAGFlow `pomelo_delivery` write. The contract is the only hand-maintained source for integrated topology configuration.

## Topology

| Role | Application code | Version label | Components | Service | External network hostname |
| --- | --- | --- | --- | --- | --- |
| `RAGFlow Integrated CPU` | `ragflow-integrated` | `ragflow-integrated-cpu` | `mysql`, `redis`, `minio`, `es01`, `tei`, `ragflow-cpu` | `default` | `ragflow-ragflow-cpu` |
| `RAGFlow Integrated GPU` | `ragflow-integrated` | `ragflow-integrated-gpu` | `mysql`, `redis`, `minio`, `es01`, `tei`, `ragflow-cpu` | `default` | `ragflow-ragflow-cpu` |

Every Component joins the external `traefik` network. Hostnames and Version labels above are deployment contracts, not compatibility suggestions.

## Model Caches

Run the selected CPU or GPU TEI preflight against every path below before deployment:

- `data/deployment/ragflow-integrated/default/tei/cache/bge-m3`
- `data/deployment/ragflow-split/default/tei/cache/bge-m3`

## Runtime Config Primitive

IntegratedRuntimeConfig = { MYSQL_PASSWORD, REDIS_PASSWORD, MINIO_USER, MINIO_PASSWORD, ELASTIC_PASSWORD }

Runtime values are never stored in this contract. Create a single in-memory value object for a new complete resource set; retain matching authorized values when reusing a resource.

| Service | Required runtime keys |
| --- | --- |
| `ragflow-integrated`/`default` | `MYSQL_PASSWORD`, `REDIS_PASSWORD`, `MINIO_USER`, `MINIO_PASSWORD`, `ELASTIC_PASSWORD` |

## Component Primitive

| Component | Image | Command | Logical mount | Network alias |
| --- | --- | --- | --- | --- |
| `mysql` | `mysql:8.0.39` | `--max_connections=1000 --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --default-authentication-plugin=mysql_native_password --tls_version=TLSv1.2,TLSv1.3 --binlog_expire_logs_seconds=604800` | bind `mysql/data` -> `/var/lib/mysql` | `ragflow-mysql` |
| `redis` | `valkey/valkey:8` | `sh -c exec redis-server --requirepass "$${REDIS_PASSWORD}" --maxmemory 128mb --maxmemory-policy allkeys-lru` | bind `redis/data` -> `/data` | `ragflow-redis` |
| `minio` | `pgsty/minio:RELEASE.2026-03-25T00-00-00Z` | `server --console-address :9001 /data` | bind `minio/data` -> `/data` | `ragflow-minio` |
| `es01` | `elasticsearch:8.11.3` | `default` | bind `es01/data` -> `/usr/share/elasticsearch/data` | `ragflow-es01` |
| `tei` | cpu: `ghcr.io/huggingface/text-embeddings-inference:cpu-1.9.3@sha256:ad950d30878eceb72aaf32024d26fa2b1d04a75304fa0b4776b49aa1941fea07`; gpu: `ghcr.io/huggingface/text-embeddings-inference:cuda-1.9.3@sha256:249a0bc87522bfe2f1012b4d194f0225878f47079115ada3aeb0b1ef257b402a` | `--model-id /data/bge-m3 --json-output` | bind `tei/cache` -> `/data` | `ragflow-tei` |
| `ragflow-cpu` | `infiniflow/ragflow:v0.26.4` | `--enable-adminserver --init-model-provider-tables` | bind `ragflow-cpu/logs` -> `/ragflow/logs` | `ragflow-ragflow-cpu` |

### Component environment

| Component | Fixed environment | Runtime keys |
| --- | --- | --- |
| `mysql` | MYSQL_DATABASE=rag_flow, MYSQL_ROOT_HOST=% | `MYSQL_PASSWORD` |
| `redis` | none | `REDIS_PASSWORD` |
| `minio` | none | `MINIO_USER`, `MINIO_PASSWORD` |
| `es01` | node.name=es01, bootstrap.memory_lock=false, discovery.type=single-node, xpack.security.enabled=true, xpack.security.http.ssl.enabled=false, xpack.security.transport.ssl.enabled=false, cluster.routing.allocation.disk.watermark.low=5gb, cluster.routing.allocation.disk.watermark.high=3gb, cluster.routing.allocation.disk.watermark.flood_stage=2gb | `ELASTIC_PASSWORD` |
| `ragflow-cpu` | DOC_ENGINE=elasticsearch, DEVICE=cpu, API_PROXY_SCHEME=python, TZ=Asia/Shanghai, MYSQL_HOST=mysql, MYSQL_PORT=3306, MYSQL_DBNAME=rag_flow, MYSQL_USER=root, MYSQL_MAX_PACKET=1073741824, REDIS_HOST=redis, MINIO_HOST=minio, ES_HOST=es01, ES_USER=elastic | `MYSQL_PASSWORD`, `REDIS_PASSWORD`, `MINIO_USER`, `MINIO_PASSWORD`, `ELASTIC_PASSWORD` |

### Health and special settings

| Component | Health check | Special settings |
| --- | --- | --- |
| `mysql` | `CMD-SHELL mysqladmin ping -uroot -p"$${MYSQL_ROOT_PASSWORD}"` | none |
| `redis` | `CMD-SHELL redis-cli -a "$${REDIS_PASSWORD}" ping` | none |
| `minio` | `CMD curl -fsS http://localhost:9000/minio/health/live` | none |
| `es01` | `CMD-SHELL curl -fsS -u "elastic:$${ELASTIC_PASSWORD}" http://localhost:9200` | tmpfs /tmp:size=536870912,mode=1777; ulimits {"memlock":{"soft":-1,"hard":-1}}; resources {"resources":{"limits":{"memory":"8g"}}} |
| `tei` | `CMD-SHELL curl -fsS http://127.0.0.1:80/health` | profile overrides {"gpu":{"deploy":{"resources":{"reservations":{"devices":[{"driver":"nvidia","count":"all","capabilities":["gpu"]}]}}}}} |
| `ragflow-cpu` | `CMD curl -fsS http://localhost:80/` | none |

## Endpoint Primitive

```json
[
  {
    "component_name": "mysql",
    "name": "mysql",
    "protocol": "tcp",
    "container_port": 3306,
    "mode": "internal"
  },
  {
    "component_name": "redis",
    "name": "redis",
    "protocol": "tcp",
    "container_port": 6379,
    "mode": "internal"
  },
  {
    "component_name": "minio",
    "name": "api",
    "protocol": "http",
    "container_port": 9000,
    "mode": "internal"
  },
  {
    "component_name": "es01",
    "name": "http",
    "protocol": "http",
    "container_port": 9200,
    "mode": "internal"
  },
  {
    "component_name": "tei",
    "name": "http",
    "protocol": "http",
    "container_port": 80,
    "mode": "internal"
  },
  {
    "component_name": "ragflow-cpu",
    "name": "http",
    "protocol": "http",
    "container_port": 80,
    "mode": "gateway_http",
    "entrypoint": "web"
  }
]
```

## Manual Provider Initialization

Configure this HuggingFace embedding provider manually in the RAGFlow UI after the selected Version is healthy:

| Name | Model | Base URL | Max tokens |
| --- | --- | --- | --- |
| `tei-bge-m3` | `BAAI/bge-m3` | `http://tei:80` | 8192 |

`TEI_BASE_URL` is a manual Provider setting, not a `ragflow-cpu` container environment variable.

## MCP Workflow Primitive

```text
discover:  orbit_list_projects -> orbit_list_applications -> orbit_get_application
           -> orbit_list_versions / orbit_get_version -> orbit_list_application_services
preflight: runtime_doctor(network_name="traefik")
write:     orbit_create_application -> orbit_create_version
           -> orbit_create_service -> orbit_preview_service -> orbit_deploy
verify:    orbit_wait_deployment -> runtime_compose_ps -> runtime_http_probe
           -> verify_deployment
```

Inspect each write tool schema immediately before use. Use `orbit_update_*` only to repair the matching target resource, and sanitize tool results before reporting them.
