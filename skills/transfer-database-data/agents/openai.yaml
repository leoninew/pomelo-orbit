interface:
  display_name: "Transfer An Orbit Service"
  short_description: "Move one Orbit service through dbtalk JSONL"
default_prompt: "Use $transfer-database-data to export or import one Orbit service deployment closure for the current selected Project through the dbtalk CLI."
