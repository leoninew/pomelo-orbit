interface:
  display_name: "Transfer Orbit Data"
  short_description: "Move service closures or restore one Environment"
  default_prompt: "Use $transfer-database-data to back up one Orbit Environment with its SSH key and restore it to a specified Project."
